# JAT-AI Fork — Agent Prompts

Prompts for dispatching Jules "developer agent" sessions against your JAT-AI fork, one per build-order step. Each is meant to be pasted as the `prompt` field when creating a session, on its own branch, against the actual fork repo — not the control-plane repo.

Grounded against the real source (`account_pool.py`, `coordinator.py`, `workflow_engine.py`, `session_runner.py`, `auto_merge.py`, `models/workflow.py`, `prompts/system_prompts.py`, `supabase/schema.sql`, `clients/jules.py`, `context_store.py`, `tracker.py`, `prompt_builder.py`, `clients/github.py`, `mcp/server.py`, `plan_executor.py`, `config_loader.py`, `api/chat.py`, `api/plans.py`, `rag_store.py`, `merge_review.py`) rather than the README's summary. Where a file wasn't available, the prompt says so and tells the agent to inspect first instead of guessing. Earlier research was done against the original `iceyxsm/JAT-AI` upstream; `merge_review.py` and everything found through it came from the actual working fork (`ProjectCoCom/Hirschberg`, `development` branch) once implementation moved there — if a step references a repo-specific detail that seems off, that's why.

Run these **in order, one at a time**. They touch overlapping files throughout; dispatching them in parallel will produce merge conflicts between your own agents. Merge each PR before starting the next.

**Step 4 is a harder dependency than the numbering alone suggests.** It retires `plan_executor.py`, a second, previously-unknown execution engine that runs independently of everything else in this document — including an unconditional auto-approve of every plan, which means Step 7's whole point (a human approval gate) silently does nothing until Step 4 is actually merged. Don't skip ahead to 7 while 4 is still pending. Step 13 has the same dependency for a related reason — see its context.

1. Fork JAT-AI — just `git clone`, no prompt needed.
2. Remove Supabase, complete local database functionality.
3. Account roles + retire ai_providers planning.
4. Consolidate onto one execution engine (retires plan_executor.py's parallel system).
5. Wake-up loop: status relay, mid-task questions, fail-fast fix.
6. QA gate.
- 6.1. Integration review + final PR (separate integrator role).
7. requirePlanApproval + audit trail.
8. Recursive dispatch to sub-orchestrators (stretch).
9. Backpressure-aware dispatch (performance/correctness).
10. SQLite write-concurrency tuning (performance).
11. Capacity-aware account routing (performance).
12. Query pattern fixes: context_store.py and tracker.py (performance).
13. Consolidate session polling into one shared engine (performance).
14. Reduce auto_merge.py's check-polling overhead (performance).
15. Fix the MCP server's per-call event loop creation (performance/correctness).
16. Cache prompt_builder.py's template reads (performance).
17. File header comments + docs/map.json (repo navigability).

UI changes are intentionally excluded — that's your own work, last.

Steps 9-16 are additive fixes to functions Steps 2, 3, 5, and 6 already touch. Dispatch them
after 2-8 (and 6.1) are merged, not interleaved with them, for the same merge-conflict reasons
as everything else in this document. Step 12 specifically should run after Step 2, since it
depends on Step 2's decision about context_messages' upsert conflict target. Step 13 is worth
considering out of order: if Steps 5 and 7 haven't been dispatched yet, doing Step 13 first
means they only touch one polling implementation instead of two — but Step 13 also depends on
Step 4 being complete (see its context), so it can't move earlier than that regardless. Step
15 is independent of everything else and can run any time. Step 16 is independent too, but
touches the same file Step 3 redirects away from for planning (prompt_builder.py) — fine to
run either order, just not simultaneously. Step 6.1 depends on Step 6 and should build against
whichever polling mechanism actually exists at the time — Step 5's original dual loops if
Step 13 hasn't landed yet, or Step 13's consolidated engine if it has; don't build a third one.
Step 17 should run last of all, after everything above — it writes descriptive headers into
every file, and running it before the build order finishes just means redoing that work as
later steps keep changing those same files.

---

## Step 2 — Remove Supabase, complete local database functionality

```
Replace Supabase with a fully-functional local SQLite database. No Supabase configuration,
client, or dependency should remain anywhere in the codebase.

CONTEXT
supabase/schema.sql defines at least twelve Postgres tables — treat this as a floor, not a
ceiling; two more (plans, token_usage) turned up in files outside this document's original
scope, so there may be others: accounts, account_sources, workflows, agent_tasks,
context_messages, merge_queue, session_activities, ai_providers, conversations,
conversation_messages, prompts (a seeded prompt library — security-audit, add-tests,
code-review, new-project, fix-issues), plans (conversation_id, title, plan_json, status —
draft plans saved from chat, see Step 4), and token_usage. It uses uuid_generate_v4()/
gen_random_uuid(), jsonb columns, a native uuid[] array column (agent_tasks.depends_on),
timestamptz, an idempotent index-creation block wrapped in "do $$ ... $$", and RLS-disable
statements at the end. Get an actual full table list directly from the file rather than
trusting this one to be complete.

config.example.json already has a database.mode setting with a "local" option pointing at
./data/jat.db and a sync_interval value — inspect src/config.py and src/clients/supabase.py
first to see how much of local mode already works versus how much is a stub, and what the
sync_interval is actually doing (it may imply local currently syncs to Supabase rather than
standing alone — confirm before assuming).

Resolved, not a risk after all: core/rag_store.py (used by api/chat.py for conversation/
context retrieval) runs on ChromaDB, a self-contained, file-based embedded vector store
(data/chromadb/) — not pgvector, not Postgres at all. It's already independent of whichever
relational database the rest of the app uses, already degrades gracefully to a no-op if
chromadb isn't installed, and needs no changes as part of this step.

Several files call a generic db.update(table, values, filter) / db.upsert(table, values) /
db.select(table, filters) / db.insert(table, values) / db.delete(table, filters) interface on
a SupabaseClient instance (confirmed in session_runner.py — search for the same pattern
elsewhere). If clients/supabase.py's full interface is a plain table/filter CRUD surface like
this, a SQLite-backed client implementing the identical method signatures can be a largely
mechanical swap at most call sites.

REQUIREMENTS
- Inspect src/clients/supabase.py fully first to get its complete method surface (not just
  update/upsert — also whatever select/read methods exist).
- Build src/clients/local_db.py implementing the same method surface against SQLite, so
  existing call sites need only a different import and constructor, not rewritten logic.
- Translate supabase/schema.sql to SQLite as a new local schema file, table for table:
    - UUID primary/foreign keys -> TEXT columns holding UUID strings, generated in Python
      (uuid4()) at insert time — SQLite has no native UUID generation.
    - jsonb columns (context_messages.message/context, session_activities.artifacts/raw_data,
      agent_tasks.context, conversation_messages.metadata, etc.) -> TEXT columns holding JSON
      strings.
    - agent_tasks.depends_on (currently a native uuid[] array) -> a JSON-encoded TEXT array,
      for consistency with every other JSON column above rather than a separate join table.
      Keep the application-level shape (a list of UUID strings) identical — only the storage
      encoding changes.
    - timestamptz -> TEXT storing ISO-8601, default (datetime('now')).
    - The "do $$ ... $$" idempotent index block -> plain CREATE INDEX IF NOT EXISTS
      statements, which SQLite supports directly with no wrapper needed.
    - "on conflict (...) do nothing" seed inserts -> SQLite supports the same ON CONFLICT
      syntax; keep the seeded prompts library (security-audit, add-tests, code-review,
      new-project, fix-issues) intact.
    - Drop the RLS-disable statements entirely — SQLite has no row-level security model, they
      don't translate to anything.
    - Ensure the app's SQLite connection setup runs PRAGMA foreign_keys = ON — SQLite doesn't
      enforce declared foreign keys unless this is set per connection.
- accounts' API key storage: confirmed via plan_executor.py and api/chat.py, not a guess
  anymore — it's read as a column (seen as api_key_encrypted in both places; check whether
  this is the same column referenced elsewhere as api_key_hash or whether both exist for
  different purposes) and decrypted through core.ai_interface.KeyVault.decrypt() before use.
  This is reversible, symmetric encryption, not a one-way hash. api/chat.py's _decrypt_key()
  also strips a leading "\x" before decrypting — that's Postgres's own hex representation of
  a bytea column when cast to text, not part of KeyVault's actual encryption format. That
  workaround is Postgres-specific and becomes dead weight (or a bug, if it strips a real
  leading "\x" out of a value that happens to start that way) once this runs against SQLite,
  which doesn't return binary columns that way — don't carry it forward unchanged. The local
  replacement must use the same KeyVault-based approach with the same key material handling —
  don't simplify this into a true one-way hash (which would break the ability to retrieve a
  usable key) or plaintext storage.
- tracker.py's subscribe_task_updates(), subscribe_activity_updates(), and
  subscribe_workflow_tasks() are built entirely on Supabase's realtime feature
  (self._db.client.channel(...).on_postgres_changes(...).subscribe()) — a websocket-based
  Postgres change feed with no SQLite equivalent. These will break the moment Supabase is
  removed unless replaced. Build an in-process pub/sub mechanism instead: local_db.py's
  insert/upsert methods should fire an in-process callback immediately after a successful
  write to the affected table, and tracker.py's three subscribe_* methods should register
  against that in-process emitter instead of a Supabase channel — same external interface
  (register a callback against a table, optionally filtered by workflow_id), different
  mechanism underneath. Separately, confirm whether the dashboard's frontend currently
  subscribes to Supabase realtime directly from the browser rather than through tracker.py —
  if it does, the app needs its own WebSocket or Server-Sent-Events endpoint to relay these
  in-process events to connected browser clients, since local SQLite has no browser-reachable
  equivalent to Supabase's hosted realtime infrastructure. Don't assume the in-process fix
  alone covers this without checking.
- context_store.py's save_result() upserts into context_messages using only task_id, with no
  visible unique constraint on that column in the current schema and no explicit ordering
  when it's read back (get_dependency_context takes rows[0], implicitly assuming either "one
  row per task_id" or "first row is authoritative"). Decide this explicitly during the schema
  translation: add a unique constraint on task_id if per-task upsert-not-insert is the
  intended behavior, and make sure local_db.py's upsert() actually enforces it — don't carry
  forward an implicit assumption that happened to hold by accident under Supabase's ordering.
- Remove the supabase section from config.example.json. Grep the whole codebase (not just
  files already named in this build order) for "clients.supabase" / "from clients import
  supabase" and remove every reference, including the sync_interval concept if it turns out
  to be Supabase-sync-specific. Confirmed concrete example: mcp/server.py's _get_db() builds
  a SupabaseClient directly from os.environ["SUPABASE_URL"]/["SUPABASE_KEY"] — this needs to
  point at local_db.py instead, or it'll crash outright the moment those env vars are gone.
- Make local the only mode — remove the mode switch entirely, don't leave a dead branch.

CONSTRAINTS
- Don't change table shapes beyond what SQLite requires — role/label columns for accounts are
  the next step's job, not this one.
- Keep the db interface identical to what callers already use, so touching those call sites
  is mechanical (swap the import/constructor) everywhere except local_db.py itself.

ACCEPTANCE CRITERIA
- The application runs end to end — account creation, a workflow with dependent tasks,
  session polling, activity storage, merge — with zero Supabase configuration present and no
  supabase package imported anywhere.
- PRAGMA foreign_keys is confirmed on, and an invalid foreign key insert is rejected.
- Subscribing to task/activity updates through tracker.py's three subscribe_* methods still
  delivers live callbacks with Supabase fully removed — verify this directly, not just that
  the methods don't error.
- A second save_result() call for the same task_id updates the existing context_messages row
  rather than creating a duplicate, and get_dependency_context returns the current value.
- Existing tests pass against the local DB; add a test confirming a workflow's tasks and
  their depends_on relationships round-trip correctly through the JSON-array encoding.
```

---

## Step 3 — Account roles + retire ai_providers planning

```
Add a role field to Jules accounts and stop routing plan construction through the ai_providers
pool.

CONTEXT
src/core/account_pool.py currently defines:

    class PlanTier(StrEnum):
        FREE = "free"
        PRO = "pro"
        ULTRA = "ultra"

    @dataclass
    class Account:
        id: UUID = field(default_factory=uuid4)
        name: str = ""
        api_key: str = ""
        plan: PlanTier = PlanTier.FREE
        active_sessions: int = 0
        daily_tasks_used: int = 0
        daily_reset_at: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
        sources: list[str] = field(default_factory=list)
        enabled: bool = True

There is no role concept yet. AccountPool.acquire(source=None) filters to accounts with
has_capacity, optionally narrows to accounts whose sources list contains the requested source
(coordinator.py calls this with source formatted as "sources/github/{owner}/{repo}") — but
falls back to the full eligible list if none match, a soft preference, not a hard filter —
sorts by active_sessions ascending, and returns the least-loaded match.

Separately, src/prompts/system_prompts.py defines PLAN_MODE_SYSTEM and AUTO_MODE_SYSTEM: full
system prompts that make an ai_providers chat model (Groq/Gemini/etc.) act as "JAT-AI" and
decompose a goal directly into a JSON task plan with the user, entirely independent of Jules.
This is the mechanism we're retiring — plan authorship becomes the standing orchestrator
Jules session's job (Step 4), not a chat model's.

REQUIREMENTS
- In src/core/account_pool.py, add an AccountRole StrEnum next to PlanTier:

    class AccountRole(StrEnum):
        ORCHESTRATOR = "orchestrator"
        WORKER = "worker"
        QA = "qa"

- Add two fields to the Account dataclass, following its existing style:
    role: AccountRole = AccountRole.WORKER
    label: str = ""

- Change AccountPool.acquire() to accept role: AccountRole | None = None, applied before the
  existing source filter. Unlike source, which is a soft preference that falls back to the
  full eligible list when nothing matches, role must be a hard requirement: if role is given
  and no eligible account has that role, raise AccountPoolExhausted with a role-aware
  message. Never fall back to returning an account of the wrong role.
- Update coordinator.py's account_pool.acquire(source) call in run_task() to also pass
  role=AccountRole.WORKER explicitly, always, with no conditional logic. At this stage,
  run_task() only ever handles ordinary coding tasks — QA review (Step 6) and orchestrator
  delegation (Step 8) don't exist yet and will need their own account-selection logic when
  those steps are built. Don't add speculative branching for either now.
- Add role to the dict returned by AccountPool.status(), alongside the existing fields — this
  is what the dashboard's account table will read.
- config_loader.py's build_jules_pool() is the confirmed function that constructs Account
  objects at startup and calls add_account() for each — pass role/label through here.
  It currently sources accounts from config.json's jules.accounts[], which is a separate,
  disconnected list from the accounts DB table the dashboard actually writes to (confirmed:
  plan_executor.py reads accounts straight from that table via a completely different path).
  Change build_jules_pool() to read from the accounts table instead of config.json, so
  there's one source of truth for accounts feeding account_pool.py — an account added through
  the dashboard should be visible here without also needing a config.json edit.
  On first run after this change, if the accounts table is empty and config.json still has
  jules.accounts entries, copy them into the table once — insert each as a row, log clearly
  that a one-time migration happened — and never read config.json for accounts again after
  that point, even if the table is later emptied intentionally (e.g. all accounts deleted
  through the dashboard). This is a one-time migration, not an ongoing fallback: don't
  re-check config.json on every subsequent startup, and don't repopulate the table just
  because it happens to be empty later. If the table is empty and config.json has nothing
  either, start with an empty pool — accounts get added through the dashboard from there.
- Add role and label columns to whichever local SQLite schema Step 2 produced (not
  supabase/schema.sql, which Step 2 removes) — match its conventions, default existing rows
  to "worker".
- Update the dashboard's existing account form/table to expose role (dropdown) and label
  (text). Match the current form's patterns exactly — don't restyle anything else.
- No change needed to how plan/auto mode chat works — api/chat.py's chat_send() (ask/plan/
  build/auto, RAG context, summarization, skills injection, multi-key ai_providers failover)
  is confirmed to be legitimate, general-purpose chat infrastructure with nothing specific to
  retire here. What actually changes is what happens to a saved plan once someone wants it
  executed, which is Step 4's job, not this one — see Step 4's context for the full picture.

CONSTRAINTS
- Don't touch workflow_engine.py, session_runner.py, or anything outside
  account/config/schema/dashboard-form scope.
- Don't redesign the dashboard visually.
- Preserve acquire()'s existing behavior exactly when role is not passed (None).

ACCEPTANCE CRITERIA
- An operator can tag each connected account as orchestrator/worker/qa from the dashboard.
- acquire(role=AccountRole.WORKER) never returns an orchestrator- or qa-tagged account, and
  raises AccountPoolExhausted — not a silent fallback — when none are available.
- Existing calls to acquire() with no role argument behave exactly as before.
- Chat-based plan drafting (api/chat.py's plan/auto modes) still works exactly as before —
  this step doesn't touch it.
- On a fresh accounts table with existing config.json accounts, restarting the app once
  populates the table and logs the migration; restarting a second time does not re-read
  config.json or duplicate rows, even after manually deleting all rows from the table between
  restarts.
- Existing tests pass; add tests covering role-filtered acquisition, including the
  no-fallback-on-miss behavior.
```

---

## Step 4 — Consolidate onto one execution engine

```
There are two independent, non-integrated systems for running Jules tasks in this codebase.
Consolidate onto the one with persistence, retries, and dependency-aware parallelism, and
retire the other's execution machinery.

CONTEXT
System A: workflow_engine.py + coordinator.py + account_pool.py + clients/jules.py +
context_store.py. Accounts sourced from the accounts table via config_loader.build_jules_pool()
(Step 3). Tasks persisted to agent_tasks/workflows via the shared db interface. Proper retries
(tenacity), proper connection reuse (per-account cached JulesClient), dependency-aware parallel
execution, and — once Steps 2, 3, 5, 6, 7, 9, 11, and 13 land — local-DB backed, role-aware,
capacity-aware, QA-gated, and backpressure-safe. Every other step in this document builds on
top of this system.

System B: core/plan_executor.py, triggered via /api/execute — previously unlocated despite
checking api/chat.py (confirmed to only save drafts, never execute) and api/plans.py
(confirmed plain CRUD, no execute action). parse_plan() reads the same JSON shape
PLAN_MODE_SYSTEM/AUTO_MODE_SYSTEM produce:

    {
      "tasks": [
        {
          "id": "agent-1",
          "description": "What this agent does",
          "prompt_id": "skill-name-from-available-skills-or-null",
          "dependencies": [],
          "exit_criteria": "How to verify this is done",
          "branch": "jat/agent-1-description"
        }
      ],
      "execution_mode": "hybrid"
    }

into plan_executor's own lightweight AgentTask dataclass — structurally different from
models/workflow.py's AgentTask (string IDs vs UUIDs, no workflow_id/parent_task_id/
account_id, no TaskStatus enum). From there, System B runs entirely on its own:
- get_jules_key() picks an account by querying the accounts table directly and taking
  whichever enabled row has the lowest sessions_today — no role awareness, no source
  matching, nothing from account_pool.py. Once Step 3 adds a role column, this function has
  no way to know it does.
- create_jules_session(), send_message_to_session(), _approve_plan(), poll_session_status(),
  and _fetch_recent_activities() all make raw httpx calls straight to
  https://jules.googleapis.com, duplicating clients/jules.py's JulesClient with less
  robustness — most have no retry logic at all.
- poll_session_status() is a fourth independent session-polling loop, on top of the two Step
  13 consolidates and the mcp/server.py call site Step 13 confirmed delegates to one of them.
- poll_session_status() auto-approves every AWAITING_PLAN_APPROVAL unconditionally — no
  condition, no human involved. This is the exact state Step 7 adds human gating for. Left
  running, Step 7's gate does nothing for anything dispatched through System B.
- poll_session_status() answers a worker's mid-task question via _answer_jules_question(),
  which calls the ai_providers pool directly. This is the real call site Step 5 redirects.
- None of System B's task execution touches agent_tasks, workflows, context_messages, or
  session_activities — only accounts.sessions_today gets written. Nothing dispatched this way
  is visible to the dashboard's task views, Step 7's audit trail, or Step 6's QA gate.
- create_jules_session() builds its prompt directly, without going through prompt_builder.py
  — sessions dispatched this way never get the rules/gates/anti-patterns/context injection
  Step 16 caches. They get a bare prompt.

Beyond /api/execute itself, get_jules_key(), create_jules_session(), poll_session_status(),
and reset_daily_sessions() are also imported by api/server.py (recovery/reset loops),
core/merge_review.py (review sessions — the actual implementation behind the post-merge
integration reviewer; see Step 6.1 for what happens to it), core/auto_mode.py, and
mcp/server.py. Retiring these functions has a wider blast radius than /api/execute alone.

Recommendation: retire System B's execution machinery — get_jules_key(),
create_jules_session(), send_message_to_session(), _approve_plan(), poll_session_status(),
_fetch_recent_activities(), and _answer_jules_question() — and keep only parse_plan() for
what it's actually good at: turning the chat interface's JSON output into structured data.
Retarget that output at models/workflow.py's Workflow/AgentTask shape and hand it to
workflow_engine.py instead of driving a second, weaker execution path.

REQUIREMENTS
- Add assign_to: str (account name or label) to the plan JSON shape above. Everything else
  (id, description, dependencies, exit_criteria, branch, prompt_id, execution_mode) stays
  exactly as-is.
- Change parse_plan() to build models/workflow.py's Workflow/AgentTask objects — translating
  string dependency references like "agent-1" into the UUID-based depends_on list once real
  task UUIDs are assigned — instead of plan_executor's own AgentTask dataclass.
- Redirect /api/execute to build a Workflow from parse_plan()'s output and hand it to
  workflow_engine.run(), instead of looping through create_jules_session().
- Resolve assign_to through the role-aware account_pool.py (Step 3). This is a hard
  requirement, not a preference: if assign_to names a specific account or label and no
  matching account is enabled and has capacity, acquire() raises AccountPoolExhausted — don't
  silently fall back to a different available worker the orchestrator didn't ask for.
- System A doesn't currently create git branches or update .jules/jdocs/context.xml before/
  after a task runs — System B's create_branch_from_ref() did this. Add a small helper in
  clients/github.py (not inline in coordinator.py) that creates the task's branch before
  dispatch, using a GitHub token passed through from wherever /api/execute already has one.
  For JDOCS specifically: check whether coordinator.py currently builds task prompts via
  prompt_builder.build_session_prompt() or its own separate prompt construction. If it's the
  latter, switch it to build_session_prompt() — JULES_MASTER_PROMPT already instructs the
  agent to update JDOCS itself as part of its own work, so this is likely the entire fix, not
  new orchestration logic.
- Delete get_jules_key() and poll_session_status() outright — don't preserve slimmed-down
  versions of either. They're not just old code, they contain the two specific bugs (no role
  awareness, unconditional auto-approve) this consolidation exists to fix; a smaller version
  that keeps either property just hides the bug one layer deeper.
- Refactor mcp/server.py's imports to use JulesClient/AccountPool now, as part of this step.
- For api/server.py, core/merge_review.py, and core/auto_mode.py: repoint their imports to
  JulesClient/AccountPool so they keep working, but don't redesign what they do as part of
  this step. core/merge_review.py's actual behavior is Step 6.1's territory, not this one —
  Step 6.1 keeps its integration-pipeline job but replaces its "fix things silently" behavior
  and moves it onto a dedicated integrator-role account. If auto_mode.py or api/server.py
  turn out to have their own polling loops or their own account-selection logic — plausible,
  given the pattern everywhere else in this codebase — flag that clearly rather than fixing
  it here; it belongs with Step 13 or Steps 9/11 respectively.
- Route reset_daily_sessions() callers to account_pool.py's existing daily_tasks_used/
  daily_reset_at mechanism instead — that already does the same job.
- Widen AgentTask.context from dict[str, str] to dict[str, Any] in src/models/workflow.py, so
  Step 6's QA verdict can store structured data directly.
- Add the columns this step needs (assign_to, prompt_id, exit_criteria on agent_tasks;
  execution_mode on workflows) directly into Step 2's schema file, the same way every other
  schema change in this build order works. Don't add them via runtime "check for missing
  column, alter table if absent" logic in local_db.py — that scatters what the schema actually
  is across implicit startup behavior instead of one reviewable file. If Step 2 has already
  run against a database with real data in it, write a proper follow-up migration file
  instead, matching whatever numbered-migration convention the project already uses.

CONSTRAINTS
- Don't touch how dependencies already resolves within workflow_engine.py — only add the
  assign_to -> account_id resolution and the parse_plan() retargeting.
- Malformed or partial plan output should log and wait for a corrected version, not crash
  anything downstream.
- Don't redesign core/merge_review.py's, core/auto_mode.py's, or api/server.py's actual
  behavior as part of this step — import repointing only. Flag anything that looks like it
  belongs to a later step instead of fixing it inline here.
- If retiring System B's execution functions breaks something not visible from the files
  inspected so far, stop and flag it rather than silently working around it.

ACCEPTANCE CRITERIA
- A plan dispatched via /api/execute results in real Workflow/AgentTask rows in
  agent_tasks/workflows, executed by workflow_engine.py with dependency-aware parallelism —
  not plan_executor's own loop.
- assign_to correctly resolves to account_id via the role-aware pool, and raises
  AccountPoolExhausted rather than substituting a different account when the named one isn't
  available.
- A plan dispatched this way creates its task branches, gets its JDOCS updates (via
  build_session_prompt(), confirmed not new orchestration code), and is visible in the same
  places a workflow_parallel.json-driven run already is: agent_tasks rows, session_activities
  rows, dashboard task views.
- get_jules_key() and poll_session_status() are gone, full stop — no slimmed-down versions.
- mcp/server.py, api/server.py, core/merge_review.py, and core/auto_mode.py all still run
  without error against JulesClient/AccountPool, with no behavior changes beyond that swap.
- agent_tasks and workflows have the new columns present via the schema file, not added at
  runtime.
```

---

## Step 5 — Wake-up loop: status relay, mid-task questions, fail-fast fix

```
Give the standing orchestrator session visibility into what its dispatched tasks are doing,
without stopping unrelated work every time one thing fails.

CONTEXT
Neither coordinator.py's _poll_session nor session_runner.py's _poll_until_done ever calls
Jules's sendMessage — both are one-shot "create, poll to a terminal state, return" loops.
clients/jules.py already implements send_message(session_id, prompt) — the client-level
capability exists, nothing calls it yet.

workflow_engine.py's run() currently cancels every other running task in the workflow the
moment any single task reaches FAILED — full workflow-wide cancellation, no distinction
between a failure that actually blocks a sibling and one that doesn't.

session_runner.py's _store_activity() always writes raw_data: {} — the schema has the column,
nothing populates it.

system_prompts.py's JULES_QUESTION_HANDLER currently answers a worker's mid-task question via
the ai_providers pool. With Jules as the orchestrator, that becomes the standing
orchestrator's call instead — plan_executor.py's _answer_jules_question() is the real call
site, retired as part of Step 4.

REQUIREMENTS
- Change workflow_engine.py's failure handling from workflow-wide cancellation to
  dependency-scoped cancellation: when a task reaches FAILED, cancel every task that
  transitively depends on it — the full closure, including tasks that only depend on it
  through another task that's just been cancelled, not only direct dependents — while
  independent branches keep running.
- Add a notify_orchestrator() call, fired once per state transition (not once per poll
  tick), when a tracked task reaches COMPLETED, FAILED, PAUSED, or AWAITING_USER_FEEDBACK.
  Hook this into coordinator.py's _poll_session for DAG-run tasks and session_runner.py's
  _poll_until_done for the standing orchestrator's own session — track last-seen state
  independently in each loop for now; Step 13 will consolidate this when it merges the two
  polling implementations. Include task id/description, resulting state, PR URL if any, and
  a short summary — not the full diff.
- Call the existing JulesClient.send_message(session_id, prompt) to deliver the digest above
  to the standing orchestrator's session id.
- Route a worker session reaching AWAITING_USER_FEEDBACK to the standing orchestrator via
  sendMessage, including the worker's question and enough context to identify which task
  it's about. Watch specifically for the orchestrator's next agentMessaged activity (not any
  new message generically — a userMessaged entry could be unrelated human input) and relay
  its content back to the worker session via sendMessage on the worker's session id.
  Serialize this per orchestrator session: don't send a new notification to a given
  orchestrator while a previous one is still awaiting a reply — queue it instead. Without
  that, "the next agentMessaged activity" stops being a safe way to identify which question
  it's actually answering.
- Fix _store_activity() in session_runner.py to actually populate raw_data with the real
  activity content instead of {}. Step 6's QA-verdict parsing and Step 7's audit trail both
  need this.
- Add orchestrator_session_id as a string column on agent_tasks (and a matching field on the
  AgentTask Pydantic model) to persist which standing orchestrator is responsible for which
  task — task-level, not workflow-level, since Step 8 lets a sub-orchestrator own part of a
  workflow while another orchestrator owns the rest. Index this column — the notification
  path above will be querying by it often enough that an unindexed lookup would repeat the
  same pattern Step 12 already had to fix elsewhere. Expose save_task_orchestrator() and
  get_task_orchestrator() on ContextStore for this.

CONSTRAINTS
- The relay makes no decisions from digest content — it only delivers it and relays the
  reply. Whatever happens next is the orchestrator's call.
- Preserve existing single-task polling behavior exactly when there's no standing
  orchestrator to notify (the static workflow_parallel.json CLI path) — notification is
  additive.

ACCEPTANCE CRITERIA
- A task with two independent siblings and one dependent: failing one sibling cancels only
  the dependent (and anything that depends on the dependent, transitively), not the
  independent sibling.
- Completing a tracked task fires exactly one sendMessage to the correct orchestrator
  session; two rapid transitions don't duplicate it.
- A worker session entering PAUSED also notifies the orchestrator, same as COMPLETED/FAILED/
  AWAITING_USER_FEEDBACK.
- A worker session entering AWAITING_USER_FEEDBACK results in a message to the orchestrator
  and, once it replies, a sendMessage back to the worker — with no ai_providers call
  anywhere in that path. Two workers asking questions of the same orchestrator around the
  same time don't get each other's answers.
- raw_data on a newly stored session_activities row contains the real activity payload, not
  {}.
- get_task_orchestrator() for a given orchestrator session runs against an indexed lookup,
  not a full table scan.
```

---

## Step 6 — QA gate

```
Add a mandatory, pre-merge, report-only QA review using a dedicated qa-role Jules account,
and fix the merge check it needs to rely on.

CONTEXT
auto_merge.py's _wait_for_checks() never actually blocks a merge: on failed or timed-out
checks it logs a warning and returns normally, and merge_when_ready() then merges regardless.
The "merges after CI passes" behavior described in the README isn't what the code does today.

Separately, system_prompts.py has REVIEW_SESSION_PROMPT and core/merge_review.py implements
it — an existing integration pipeline that merges every task branch in a plan into one
combined branch and reviews the result, currently allowed to silently fix what it finds. This
is a different job than this step's QA gate — QA reviews one task's diff in isolation before
it merges into that combined branch; the integration reviewer checks what happens once
several agents' work is combined, which QA never sees. They're complementary, not
competing — see Step 6.1 for what happens to the integration side. This step's QA gate does
not touch or replace core/merge_review.py.

REQUIREMENTS
- Fix _wait_for_checks() / merge_when_ready() in auto_merge.py so failed or timed-out checks
  actually prevent the merge: return a result indicating pass/fail/timeout instead of always
  falling through, and have merge_when_ready() return MergeResult(merged=False, ...) —
  matching its existing pattern for the "already merged" case — instead of calling
  merge_pull_request regardless.
- When a worker task completes with a PR (detected via Step 5), dispatch a QA session on an
  available qa-role account, sourceContext pointed at the PR's head branch, automationMode
  unset. Pass the task's exit_criteria field into the QA prompt explicitly.
- Save the file below as prompts/qa_reviewer.md and load it as this session's prompt,
  interpolating {exit_criteria} and {task_description} the same way JULES_MASTER_PROMPT's
  placeholders already get filled elsewhere:

  -----BEGIN prompts/qa_reviewer.md-----
  <identity>
  You are the QA reviewer for a pull request produced by another AI coding agent on this
  project. You did not write this code and have no stake in it being merged — your only job
  is to find every legitimate reason it should NOT ship yet.
  </identity>

  <context>
  <exit_criteria>{exit_criteria}</exit_criteria>
  <original_task>{task_description}</original_task>
  </context>

  <constraints>
  <rule>Do not fix anything. Do not write or modify code, even trivial typos.</rule>
  <rule>Do not open a PR. Your output is a verdict, nothing else.</rule>
  <rule>Never use emojis in your responses.</rule>
  </constraints>

  <review_checklist>
  <item>Check the PR against exit_criteria first, specifically and literally. Failing this is
  a blocking issue on its own.</item>
  <item>Run the existing test suite. A pass alone isn't enough — check whether the new code
  paths actually have coverage, or existing tests just didn't touch them.</item>
  <item>Look for what isn't there: missing error handling, unhandled edge cases, race
  conditions, missing input validation, silent failure modes.</item>
  <item>Look for what conflicts: does this change contradict, duplicate, or destabilize
  something elsewhere in the codebase? Check callers of anything changed.</item>
  <item>Security and secrets: this project handles API keys and tokens — check for anything
  logged, committed, or passed somewhere it shouldn't be.</item>
  <item>Be skeptical of anything that looks too clean. If a hard problem was solved in
  suspiciously few lines, check whether it was actually solved or just no longer triggers the
  specific case in the prompt.</item>
  </review_checklist>

  <output_format>
  Be specific. "Looks fine" is not a review. Every issue needs a file, a reason, and — if
  you're not certain it's actually a problem — say so plainly rather than padding the list to
  look thorough.

  End your final message with exactly this block and nothing after it:

  {
    "verdict": "approve" or "reject",
    "blocking_issues": [
      { "file": "path/to/file", "issue": "description", "severity": "blocking" or "minor" }
    ],
    "summary": "one or two sentences"
  }

  Use "reject" if there is at least one "blocking" issue. Minor issues alone don't block, but
  list them anyway.
  </output_format>
  -----END prompts/qa_reviewer.md-----

- Parse the QA session's final message for that verdict block directly during polling (the
  same point _log_activity already reads activity.agent_messaged.agent_message), rather than
  round-tripping through the DB.
- Change merge eligibility to "CI green (from the auto_merge.py fix above) AND qa_verdict ==
  approve". On reject, don't merge — send the blocking issues to the responsible orchestrator
  session via Step 5's sendMessage mechanism.
- Look the QA verdict up from persisted state (keyed by PR or task identity) inside
  merge_when_ready() itself, rather than accepting it as an optional parameter from the
  caller. mcp/server.py's jat_merge_pr tool calls merge_when_ready() directly, bypassing the
  wake-up-loop path entirely — if the QA check only fires when a caller remembers to pass a
  verdict in, that tool becomes a way to skip QA by accident. Looking it up internally means
  every call path gets the same gate with no way to forget it.
- A QA task's TaskStatus must be COMPLETED regardless of verdict — COMPLETED means "the QA
  agent finished its review," not "the PR passed." Never set it to FAILED for a reject
  verdict: workflow_engine.py treats FAILED as a cancellation trigger, and a normal review
  outcome shouldn't route through the failure path at all. Store the verdict in context, not
  in status.

CONSTRAINTS
- QA sessions never write code and never merge anything — only produce a verdict.
- The CI-check fix and the QA gate are both hard-coded policy in auto_merge.py, not something
  the orchestrator can opt out of.

ACCEPTANCE CRITERIA
- A PR with a failing or timed-out check no longer merges — test this against the current
  soft-fail behavior specifically, to confirm it's actually fixed and not just bypassed by
  the new QA check.
- A worker PR with passing CI does not merge until a QA session returns "approve".
- A "reject" verdict blocks the merge, reaches the orchestrator with the specific blocking
  issues, and leaves the QA task's own status as COMPLETED, not FAILED.
- Add test doubles for both verdicts plus a failed-CI-check case, and confirm auto_merge.py
  handles all three correctly.
- Calling merge_when_ready() the way mcp/server.py's jat_merge_pr does — directly, no verdict
  passed in — is still blocked on a pending or reject verdict, confirming there's no bypass.
```

---

## Step 6.1 — Integration review + final PR

```
Once a multi-task workflow's individual branches have all merged into a shared integration
branch, review the combined result for cross-agent issues before the final PR to the real
target branch — using a dedicated integrator-role account, not QA.

CONTEXT
merge_review.py is a full integration pipeline: merge_branches() combines every task's
individual branch into one create_integration_branch() result, run_review_session() reviews
that combined branch, cleanup_branches() removes the individual branches, and
create_final_pr() opens the one PR that goes to the real target. This is a genuinely
different job than Step 6's QA gate — QA reviews one task's diff in isolation before it
merges into the integration branch; this reviews what happens when several agents' work
gets combined, which is invisible from any single diff. They're complementary, not
competing, and Step 6 doesn't change as a result of this step.

The real problem worth fixing: run_review_session()'s prompt currently includes "fix any
conflicts or broken imports" — this reviewer is allowed to silently patch the integration
branch with nothing re-reviewing what it changed before create_final_pr() ships it. That's
the same shape of problem QA was built to prevent, one level up. Every function in this file
is also a raw httpx call straight to GitHub's API, bypassing clients/github.py entirely —
same fix as everywhere else this pattern has shown up.

REQUIREMENTS
- Add AccountRole.INTEGRATOR to the enum in account_pool.py, following the same pattern
  ORCHESTRATOR/WORKER/QA already established in Step 3.
- For workflows with more than one task: create the integration branch once, at workflow
  dispatch time (workflow_engine.py's run(), before any tasks are dispatched), based off the
  real target branch's current SHA. Persist it (a field on the workflow, e.g.
  workflows.integration_branch).
- Extend the per-task branch-creation helper Step 4 added to clients/github.py to accept an
  optional base branch, defaulting to the workflow's integration branch when one exists,
  falling back to the real target branch otherwise. For single-task workflows, skip the
  integration branch and this whole step entirely — there's nothing to integrate, and Step
  6's existing per-task gate already merges straight to the real target correctly. Don't add
  integration-branch overhead where there's only one branch.
- Task branches merging on QA approval (Step 6, unchanged) naturally merge into the
  integration branch instead of the real target for multi-task workflows, simply because
  that's the target they were given — no Step 6 code needs to change for this to be true.
- Once every task in a multi-task workflow has reached COMPLETED with its branch merged into
  the integration branch, dispatch an integrator-role session against that combined branch.
  Prompt: same adversarial, report-only, never-writes-code discipline as QA's reviewer
  prompt, but checking for cross-agent integration issues specifically — broken imports
  between what different agents touched, duplicated or conflicting implementations,
  contradictions between tasks — with each contributing task's exit_criteria included as
  reference. Remove the "fix any conflicts or broken imports" instruction entirely; this
  session never writes code, same as QA. Same structured verdict format as QA
  (approve/reject + blocking_issues).
- On integrator approval: create_final_pr() opens the PR from the integration branch to the
  real target. Extend the state-lookup-by-identity pattern Step 6 already established for
  merge eligibility to also recognize integrator verdicts for PRs whose head branch is a
  known integration branch — this PR goes through the exact same auto_merge.py gate as every
  other merge path (CI green AND verdict approve), not a separate one.
- On integrator rejection: send the blocking issues to the responsible orchestrator session
  via Step 5's sendMessage mechanism, same as a QA rejection — the orchestrator decides on a
  corrective task; the integrator never patches anything itself.
- Only after the final PR is confirmed merged, run cleanup_branches() to remove the
  individual task branches — never before a confirmed successful merge.
- Route merge_branches(), create_integration_branch(), cleanup_branches(), and
  create_final_pr() through clients/github.py instead of raw httpx calls.
- Dispatch and poll the integrator session using whichever polling mechanism actually exists
  at the time this step runs: Step 5's original two loops if Step 13 hasn't landed yet, or
  Step 13's consolidated SessionPoller if it has. Don't build a third polling implementation.

CONSTRAINTS
- Single-task workflows never create an integration branch and never dispatch an integrator
  — Step 6's existing behavior for them is unchanged.
- Integrator sessions never write code, exactly like QA.
- cleanup_branches() only fires after a confirmed successful final merge, never speculatively.

ACCEPTANCE CRITERIA
- A workflow with 3+ tasks: each task's branch merges into a shared integration branch after
  individual QA approval; once all are in, an integrator session reviews the combined
  branch; on approve, one final PR opens and merges through the standard gate; individual
  task branches are cleaned up only after that final merge is confirmed.
- A single-task workflow behaves exactly as Step 6 already specifies on its own — no
  integration branch, no integrator dispatched.
- An integrator reject routes blocking issues to the orchestrator and does not open the
  final PR.
- No raw httpx calls remain anywhere in this pipeline.
```

---

## Step 7 — requirePlanApproval + audit trail

```
Define how a standing orchestrator session actually comes into being, give a human a review
gate on its plans specifically, and make its decision history visible without building new
UI.

CONTEXT
clients/jules.py's create_session() already accepts require_plan_approval: bool = False, and
approve_plan(session_id) is already implemented. Neither coordinator.py's _poll_session nor
session_runner.py's _poll_until_done handle AWAITING_PLAN_APPROVAL as a state — anything
outside {COMPLETED, FAILED, PAUSED} just keeps polling until SESSION_TIMEOUT (30 minutes) and
returns a timeout result.

Nothing in this document so far specifies how a standing orchestrator session is actually
created. agent_tasks.orchestrator_session_id (Step 5) records which orchestrator is
responsible for a given task, but nothing tracks orchestrator sessions as records in their
own right — there's no table, no creation path, nothing to point that column at until one
exists independently of any task.

Confirm Step 4 has actually landed before this step runs: plan_executor.py's
poll_session_status() — retired in Step 4 — auto-approves every AWAITING_PLAN_APPROVAL
unconditionally, with no human involved. If it's still running, this step's approval gate
does nothing for anything dispatched through that path.

REQUIREMENTS
- Add an orchestrators table: session_id (text, primary key — the Jules session id itself,
  no separate synthetic key needed), account_id (fk to accounts), repo_owner, repo_name,
  goal (free text), status, seed_plan_id (nullable fk to plans, if this orchestrator started
  from a chat-drafted plan), parent_orchestrator_session_id (nullable, self-referential —
  unused until Step 8, but cheap to add now rather than migrate later), created_at,
  updated_at. status is a small enum of its own (e.g. OrchestratorStatus: active,
  awaiting_plan_approval, completed, failed) — not TaskStatus; an orchestrator session is not
  an agent_tasks row and shouldn't be modeled as one.
- Add a creation endpoint (api/orchestrators.py, matching the api/chat.py and api/plans.py
  naming convention — confirm against however routers actually get registered in this
  project) — POST /api/orchestrators, accepting a target repo, a goal, and an optional
  seed_plan_id. It acquires an orchestrator-role account (Step 3's account_pool.py), builds
  an initial prompt (either "here's a draft plan sketched via chat: {plan_json} — review,
  refine, and begin dispatching tasks" if seeded, or "your goal: {goal} — break this down and
  begin dispatching tasks" if not), creates the session with require_plan_approval=True, and
  inserts the resulting orchestrators row.
- In both polling loops, on AWAITING_PLAN_APPROVAL: look up the orchestrators row for this
  session_id, set its status to awaiting_plan_approval, and notify via Step 5's mechanism.
  Do not call approve_plan() from within the polling loop — keep polling normally afterward,
  waiting for the session to naturally transition once approval happens through the endpoint
  below. As a cheap defensive check: if this state is ever seen on a session_id that isn't in
  the orchestrators table, log a warning — it should be structurally impossible (worker tasks
  never set require_plan_approval), and if it happens, something upstream set the flag
  somewhere it shouldn't have.
- Add a separate endpoint (e.g. POST /api/orchestrators/{session_id}/approve-plan) that calls
  the now-retried approve_plan(session_id) directly and updates the orchestrators row's
  status back to active on success.
- Wrap approve_plan on JulesClient with the @_retry decorator, matching the pattern already
  used on most of that file's other methods.
- Add GET /api/orchestrators/{session_id}/history returning that orchestrator's
  session_activities rows in order, joined with the agent_tasks it's dispatched (via
  orchestrator_session_id) — this is a read over data Steps 4 and 5 already persist, not new
  storage.
- Expose a pending-approval count (a simple COUNT(*) on orchestrators where status =
  awaiting_plan_approval) — add it to whichever existing dashboard-state endpoint already
  aggregates session data for the frontend (get_ui_state is a reasonable guess; confirm
  against what the dashboard actually polls before adding a new endpoint for just this).

CONSTRAINTS
- No new dashboard page or visual design — expose the data, keep presentation minimal.
- Don't build Step 8's sub-orchestrator delegation here — parent_orchestrator_session_id
  exists on the schema now so it doesn't need a later migration, but nothing populates it
  until Step 8.

ACCEPTANCE CRITERIA
- POST /api/orchestrators creates a session with require_plan_approval=True and a matching
  orchestrators row, seeded correctly from a plans row when one is given.
- An orchestrator session's first plan requires explicit approval before it can dispatch
  anything, and actually resumes on approval via the dedicated endpoint rather than the
  polling loop calling approve_plan() itself.
- AWAITING_PLAN_APPROVAL on a session with no matching orchestrators row logs a warning.
- GET /api/orchestrators/{session_id}/history returns that orchestrator's activity and
  dispatched-task history correctly, using only already-persisted data.
- The pending-approval count is visible somewhere in the existing dashboard-state response.
```

---

## Step 8 — Recursive dispatch to sub-orchestrators (stretch)

```
Let assign_to resolve to another orchestrator-role account, not just a worker, so a top-level
orchestrator can delegate "coordinate this subtree" instead of only "write this code."

CONTEXT
Step 7 added an orchestrators table with its own parent_orchestrator_session_id (nullable,
self-referential) specifically reserved for this step — use that, not
models/workflow.py's AgentTask.parent_task_id, which is about task-tree structure within a
single orchestrator's own plan, not delegation between orchestrators. There's no separate
directive_parser.py module — Step 4 folded plan ingestion directly into parse_plan() and
/api/execute; wherever assign_to gets resolved to an account (Step 4's requirements,
alongside account_pool.py) is where this step's logic belongs too.

REQUIREMENTS
- Allow assign_to to resolve to an orchestrator-role account (account_pool.py, Step 3). When
  it does, create a new orchestrators row with parent_orchestrator_session_id set to the
  delegating orchestrator's session_id, and frame its initial prompt as a delegation ("you
  are responsible for this subtree of the goal: ...") rather than a coding task, using the
  same creation path Step 7 established (POST /api/orchestrators or the function underneath
  it) rather than a separate code path. It becomes a new standing orchestrator in its own
  right — Step 5's notification and Step 6's QA gate apply to whatever it eventually
  produces, same as any other branch. Whether its output also goes through Step 6.1's
  integration review the same way, or gets integrated as part of the parent orchestrator's
  own integration pass, is worth deciding explicitly if this step gets built rather than
  left implicit.
- Guard against runaway recursion: cap delegation depth with a config value (default
  conservative, e.g. 2), checked by walking parent_orchestrator_session_id, and refuse to
  resolve assign_to to an orchestrator account beyond that depth.

CONSTRAINTS
- Stretch step — don't let it block or complicate Steps 2-7 or 6.1. Build it as an additive
  capability that's off by default (max_delegation_depth: 0 disables it entirely).

ACCEPTANCE CRITERIA
- A task whose assign_to targets an orchestrator account results in a new orchestrators row
  with parent_orchestrator_session_id correctly set, created through the same path as a
  top-level orchestrator.
- max_delegation_depth: 0 fully disables the behavior with no other code path changes
  required.
```

---

## Step 9 — Backpressure-aware dispatch

```
Stop treating account-pool exhaustion as a task failure. Dispatch only as many ready tasks as
there's real capacity for, and back off gracefully when capacity is temporarily unavailable.

CONTEXT
AccountPool.acquire() raises AccountPoolExhausted when no eligible account has capacity.
coordinator.py's run_task() doesn't handle this exception specifically — it falls into the
general except-all block that marks the task FAILED. Separately, workflow_engine.py's run()
spawns an asyncio.create_task() for every ready task at once, with no check against actual
account capacity first. Combined, this means: any workflow with more tasks becoming ready
simultaneously than there's account concurrency for will have some of those tasks fail
outright — not wait, not queue, fail — purely from dispatch timing, not from anything wrong
with the task itself. After Step 5's fix, a FAILED task cancels everything that depends on
it, so a transient capacity crunch can cascade into cancelled downstream work that had
nothing to do with the actual shortage.

REQUIREMENTS
- In workflow_engine.py's run(), before spawning tasks for the current batch of ready tasks,
  check available capacity (matching each task's required role/source) against the pool.
  Only start as many concurrent asyncio tasks as there's real room for; hold the rest in the
  ready set rather than spawning them to fail. Re-check as running tasks complete and free up
  capacity — the existing FIRST_COMPLETED wait loop is the natural point to re-evaluate.
- As a backstop for the inherent race between checking capacity and actually acquiring it
  (another dispatcher could take the last slot in between), have coordinator.py's run_task()
  catch AccountPoolExhausted specifically — not let it fall into the general except-all — and
  retry with a short backoff a small, bounded number of times before treating it as a real
  failure.
- Distinguish these two failure modes clearly in whatever gets logged/surfaced to the
  orchestrator (Step 5's notification mechanism): "waiting on capacity" is not the same event
  as "this task's execution failed," and the orchestrator (or a human) shouldn't have to
  infer which one happened from a generic error string.

CONSTRAINTS
- Don't change how tasks get selected as "ready" (dependency resolution stays as-is) — only
  change how many of the ready set actually get dispatched at once.
- Keep the bounded-retry count and backoff small enough that a genuinely exhausted pool (all
  accounts near their daily cap, not just momentarily busy) still surfaces as a real,
  actionable signal rather than retrying silently for a long time.

ACCEPTANCE CRITERIA
- A workflow with more ready tasks than available account concurrency dispatches only as many
  as there's capacity for, and dispatches the rest as capacity frees up — none of them fail
  due to pool exhaustion alone.
- A task that fails for an actual execution reason (not pool exhaustion) still fails and still
  cancels its dependents, unchanged from Step 5's behavior.
- Add a test that oversubscribes the pool relative to ready tasks and confirms zero spurious
  failures, only staggered dispatch.
```

---

## Step 10 — SQLite write-concurrency tuning

```
Configure the local database from Step 2 for concurrent write access instead of leaving it on
SQLite's default journal mode.

CONTEXT
Once Step 2 lands, the orchestrator, every worker session's poll loop, QA sessions, and the
wake-up loop's status writes all read and write the same local SQLite file concurrently —
session_activities rows get written on roughly every 15-second poll tick per active session,
on top of task/workflow status updates. SQLite's default rollback-journal mode serializes all
writers; under this project's actual concurrency (a handful of simultaneous sessions, not
thousands), that may not be a real bottleneck yet, but it's cheap to fix now and expensive to
diagnose later as a source of intermittent "database is locked" errors once usage grows.

REQUIREMENTS
- On the local DB connection(s) from Step 2's local_db.py, set:
    PRAGMA journal_mode=WAL;
    PRAGMA busy_timeout=5000;
  (or a similarly reasoned timeout — pick a value and document why) so concurrent writers
  queue briefly instead of raising immediately on contention, and readers aren't blocked by
  writers.
- Inspect whether local_db.py currently opens one connection per caller/request or shares a
  single connection/pool. If it's per-caller with a high potential concurrent-connection
  count, consider whether a bounded connection pool or a single serialized writer queue fits
  this codebase's existing async patterns better — pick one, don't leave it ad hoc.
- Add indexes appropriate to the actual query patterns in coordinator.py and session_runner.py
  (e.g., agent_tasks by workflow_id and by status, session_activities by task_id and
  session_id) if Step 2's schema translation didn't already carry these over from
  supabase/schema.sql's original indexes — inspect the translated schema to confirm what
  already exists before adding duplicates.

CONSTRAINTS
- This is a configuration and indexing pass, not a schema redesign — don't change table
  shapes here.

ACCEPTANCE CRITERIA
- WAL mode is confirmed active on the local database file.
- A test that performs concurrent writes from multiple simulated sessions (e.g., several
  session_activities upserts firing at once) completes without "database is locked" errors.
```

---

## Step 11 — Capacity-aware account routing

```
Route worker/QA dispatch toward accounts with the most combined headroom — daily budget and
concurrent capacity together, both normalized so accounts on different tiers compare fairly —
not just the fewest currently-active sessions.

CONTEXT
AccountPool.acquire() currently sorts eligible accounts by active_sessions ascending only:

    eligible.sort(key=lambda a: a.active_sessions)

This spreads concurrent load well, but ignores daily_tasks_used entirely. Two accounts on the
same plan tier with the same active_sessions count get treated identically even if one is at
2 of its 15 daily tasks and the other is at 14 of 15 — the second will hit
AccountPoolExhausted for the rest of the day well before the first does.

REQUIREMENTS
- Sort by a combined score, not a strict lexicographic ordering: pure "budget first, sessions
  only as an exact-tie-breaker" was considered and rejected — remaining daily budget rarely
  lands on an exact tie across real accounts, so active_sessions would almost never actually
  influence anything, silently discarding the concurrency-spreading behavior that already
  works. Use:

      score = (remaining_daily_budget / daily_task_limit) - (active_sessions / max_concurrent)

  sorted descending. Both terms are normalized to a 0-1 range specifically so accounts on
  different plan tiers compare fairly — raw remaining-count comparison (e.g. 100 remaining on
  an Ultra account vs 13 remaining on a Free account) would always favor the higher-tier
  account by sheer magnitude even when it's proportionally more exhausted than the smaller
  one, which is the opposite of what this step is for.
- Get daily_task_limit and max_concurrent from whichever PLAN_LIMITS-based lookup
  has_capacity already performs internally — reuse that exact mechanism rather than
  introducing a new accessor. There is no limits attribute on the Account dataclass as
  established in Step 3's work; don't add one for this alone if the existing lookup already
  gets you the same values.
- This is a single change to the one .sort() call at the end of acquire() — the role filter
  (Step 3) and source filter both just narrow the eligible list before that same sort runs
  once, so no other location needs touching for this step's actual requirement.
- Optional, cheap, your call: since Step 3 already extends AccountPool.status() with role,
  consider also surfacing each account's computed remaining-budget fraction there — gives an
  operator a way to see why the pool routed where it did, from the dashboard, without needing
  to reason about the formula. Not required for this step to be complete.

CONSTRAINTS
- Don't change has_capacity's definition or the daily-reset logic — only the ordering used to
  pick among already-eligible accounts.
- Preserve existing behavior when all eligible accounts have equal remaining-budget fraction —
  should reduce to the current active_sessions-based ordering in that case (the formula
  already guarantees this: with equal budget fractions, ordering is determined entirely by
  the active_sessions term), not introduce nondeterminism.
- Not this step's concern, and don't try to solve it here: acquire()'s sort-then-mutate
  sequence isn't inherently atomic under concurrent callers — that's a race-safety question,
  not a routing-quality one, and belongs with Step 9's capacity/exhaustion handling if it
  needs addressing at all.

ACCEPTANCE CRITERIA
- Given two same-tier eligible accounts with equal active_sessions but different
  daily_tasks_used, acquire() prefers the one with more remaining daily budget.
- Given an Ultra account with high raw remaining budget but proportionally more exhausted
  than a Free account with lower raw remaining budget, acquire() prefers the proportionally
  fresher Free account — confirms the normalization, not just the raw-count case.
- Existing tests covering the current least-active-sessions behavior still pass where
  daily_tasks_used is equal across candidates.
```

---

## Step 12 — Query pattern fixes: context_store.py and tracker.py

```
Fix two query patterns that don't scale with the data they're already accumulating: an N+1
select loop in context_store.py, and a fetch-everything-then-sort-in-Python pattern in
tracker.py — without introducing a new inefficiency in the process.

CONTEXT
context_store.py's get_dependency_context() loops over each dependency task ID and issues a
separate select() call per ID. For a task with several dependencies, this is one query per
dependency instead of one query total.

tracker.py's get_recent_activities() fetches the entire session_activities table on every
call, with no filter, then sorts and slices in Python. session_activities grows continuously
(a row roughly every 15-second poll tick per active session), so this gets more expensive
every day the system runs.

REQUIREMENTS
- Change get_dependency_context() to a single query across all dependency IDs (an IN clause
  or equivalent — extend local_db.py's select() to accept a list of values for a filter
  column if it doesn't already), then reconstruct the original dep_task_ids order from the
  batch result using a {task_id: context} mapping — don't return results in whatever order
  the database happens to hand them back. Preserve the existing behavior for a dependency
  with no stored context row: it's simply absent from the output, not an error or a
  placeholder — same as today's "if rows: results.append(...)".
- This depends on Step 2's fix to context_messages' upsert conflict target and row ordering —
  confirm that landed correctly first; both the old loop and the new batched query assume
  "one row per task_id, and it's the current one."
- Add an index: CREATE INDEX IF NOT EXISTS idx_session_activities_created ON
  session_activities(created_at); — check it doesn't already exist before assuming this is
  needed, and add it wherever Step 2's idempotent schema-bootstrap function already lives, in
  the same style as the rest of that function.
- Before changing get_recent_activities() itself: check whether anything else in tracker.py
  writes to self._activity_cache — specifically, whatever Step 2's in-process pub/sub
  replacement (subscribe_task_updates and friends) hooks into on a new activity insert.
    - If self._activity_cache is already kept live by that path: get_recent_activities()
      should serve from it directly — read the first `limit` entries off the in-memory cache,
      no database query in the common case. Only fall back to a single database fetch of up
      to max_cached_activities rows, sorted by the new index, when the cache is empty (a cold
      start), to warm it — not on every call.
    - If nothing keeps it live, or it turns out to be unused elsewhere: don't try to
      backfill or synchronize it as part of this fix. Just push ordering and the limit into
      the query itself (ORDER BY created_at DESC LIMIT :limit) using the new index, and
      return that directly. Fetching max(limit, max_cached_activities) rows on every call to
      populate a cache nothing else meaningfully uses isn't solving a real problem — flag
      self._activity_cache as possibly vestigial if this turns out to be the case, rather
      than building fetch logic to keep it fed.

CONSTRAINTS
- Keep both methods' external signatures and return shapes unchanged — this is a query
  efficiency fix, not an interface change for callers in coordinator.py or the dashboard.
- Whichever branch applies to get_recent_activities(), the database should not be queried
  for the full table on every call under any circumstance — that's the one outcome to avoid
  regardless of which architecture the cache turns out to have.

ACCEPTANCE CRITERIA
- get_dependency_context issues one query regardless of how many dependency IDs it's given,
  and returns results in the same order as the input dep_task_ids — verify both with a test,
  not just correct membership.
- A dependency with no stored context is simply absent from the result, matching today's
  behavior.
- get_recent_activities returns the same results as before against a small dataset, and a
  test with a large session_activities table (a few thousand rows) confirms it no longer
  loads the full table into memory on a warm-cache call.
- If the cache is confirmed live-fed: a warm-cache call to get_recent_activities() makes zero
  database queries, verified directly (not just by output correctness).
```

---

## Step 13 — Consolidate session polling into one shared engine

```
Replace coordinator.py's _poll_session and session_runner.py's _poll_until_done — two
separate implementations of the same "poll a session to a terminal state" loop — with one
shared engine, and give it adaptive backoff instead of a fixed interval.

CONTEXT
Both loops do fundamentally the same thing: call get_session() and list_activities() on a
timer, store new activities, and return once the session reaches a terminal state. They
differ in interval (coordinator.py polls every 30 seconds, session_runner.py every 15) for no
stated reason, and any fix has to be applied twice.

Note: coordinator.py's original _poll_session never stored individual activities at all —
only session_runner.py's loop did. Unifying onto one engine means coordinator.py's DAG-task
path will start populating session_activities for the first time. This is intentional and
correct, not an incidental side effect — Step 6's QA verdict parsing and Step 7's audit trail
both read from session_activities, and without this, every task running through the DAG
engine (the primary path once Step 4 lands) would be invisible to both. The extra write
volume this creates is exactly what Step 10's WAL-mode work was already sized for.

mcp/server.py's jat_run_session tool was a candidate for a third independent implementation,
but it isn't — it calls core.session_runner.run_session() directly, so it's already covered
by whatever this step does to session_runner.py.

plan_executor.py's poll_session_status() is a real fourth one, but it isn't being folded in
here — Step 4 retires it entirely as part of retiring System B's execution machinery. If
Step 4 hasn't landed yet when this step runs, confirm that first.

REQUIREMENTS
- Extract the shared logic into one polling engine (e.g. src/core/session_poller.py) with no
  dependency on AccountPool or ContextStore at all — its constructor takes only a Jules
  client, a session id, and an on_transition(state, activities) callback. Polling is purely
  "watch state, detect transitions, invoke the callback" — what happens in response to a
  transition (releasing an account, saving task state, triggering auto-merge) is entirely
  the caller's responsibility, supplied via that callback, not something the poller resolves
  or constructs on its own. Each caller (coordinator.py, session_runner.py) builds its
  callback closure once, at the point it already has pool/store/whatever else it needs — not
  lazily, not reconstructed per state change.
- The callback contract must preserve every side effect coordinator.py's and
  session_runner.py's existing polling loops currently produce on each relevant transition —
  releasing the account, save_result/save_task_state, setting the completion event
  (coordinator.py); storing activities, triggering auto-merge (session_runner.py) — just
  invoked from the shared engine's callback instead of inline in two separate hand-written
  loops.
- Store activities via whatever Step 2 landed as the local equivalent of _store_activity, for
  every caller uniformly — this is what closes the gap described above.
- Detect terminal states (COMPLETED, FAILED, PAUSED) and AWAITING_PLAN_APPROVAL /
  AWAITING_USER_FEEDBACK (per Step 7 and Step 5's fixes) as first-class transitions the
  callback receives, not just terminal-vs-not.
- Replace the fixed poll interval with adaptive backoff: initial_interval=2.0,
  multiplier=1.5, max_interval=30.0. Not 15.0: for a full-duration (30-minute) session, a
  15.0 cap works out to roughly 123 total polls — worse than session_runner's old flat 15s
  rate (120) and far worse than coordinator's old flat 30s rate (60), which defeats the
  point of this step for exactly the case where call volume matters most. A 30.0 cap works
  out to roughly 66 polls for the same duration — close to coordinator's old, more efficient
  baseline, while still keeping the fast 2.0s start for quick sessions. Optional: add small
  random jitter to each computed interval to avoid multiple concurrently-started sessions
  polling in lockstep.
- Once this exists, Step 5's notify_orchestrator() hook and Step 7's AWAITING_PLAN_APPROVAL
  handling should each need to be wired in one place, not two.

CONSTRAINTS
- This is a structural consolidation, not a behavior change beyond the backoff and the
  activity-storage unification described above — every other state transition coordinator.py
  and session_runner.py currently detect and act on needs to keep being detected and acted on
  exactly the same way, just from one implementation.
- The poller itself never imports or constructs AccountPool or ContextStore.

ACCEPTANCE CRITERIA
- coordinator.py's DAG-task execution and session_runner.py's standalone session execution
  (and therefore mcp/server.py's jat_run_session) all run through the same polling engine,
  with no behavior regressions in any of them — including every existing side effect
  (account release, task state persistence, auto-merge triggering) still firing correctly.
- A short-lived session (completes within the first few seconds) is detected meaningfully
  faster under the new backoff than the old flat-15s interval would have, with a comparable
  or better poll count — not worse.
- A full-duration (timeout-length) session uses meaningfully fewer total polls than either
  old flat interval — verify against actual numbers (~66 vs. session_runner's old ~120), not
  just "fewer than 15s flat."
- coordinator.py's DAG-task path now produces session_activities rows where it previously
  produced none.
- SessionPoller's constructor takes no pool or store argument — verify this structurally
  (e.g. a test asserting its signature), not just by behavior.
- Existing tests for both call sites pass against the consolidated engine.
```

---

## Step 14 — Reduce auto_merge.py's check-polling overhead

```
Cut the number of GitHub API calls auto_merge.py makes per merge by backing off instead of
polling at a fixed interval, and fix a retry gap on the one call that matters most.

CONTEXT
auto_merge.py's _wait_for_checks() polls list_check_runs() every CHECK_POLL_INTERVAL (30
seconds) for up to CHECK_TIMEOUT (600 seconds) — 20 GitHub API calls for a single merge's
check-wait, even though most of that waiting time is CI actually running, not something that
needs checking every 30 seconds. With multiple PRs merging around the same time (worker + QA
+ the Step 6.1 integrator, all producing/reviewing PRs in parallel), this adds up against
GitHub's per-token rate limit — a shared budget across the whole app, not per-session.

clients/github.py already has rate-limit awareness (_check_rate_limit()) and wraps
get_pull_request, list_check_runs, and list_pr_comments in a tenacity-based @_retry decorator
— but merge_pull_request itself isn't decorated. That's backwards: the read-only polling
calls retry automatically, but the merge itself doesn't, so a transient 5xx on a fully
approved, QA-passed, CI-green PR fails outright instead of retrying.

No webhook infrastructure exists in this codebase, so backoff is the pragmatic fix, not
webhooks (which would be new infrastructure — a publicly reachable endpoint, signature
verification — not a small addition).

REQUIREMENTS
- Apply adaptive backoff to _wait_for_checks(): initial_interval=15.0, multiplier=1.5 (same
  shape as Step 13's SessionPoller, deliberately — no reason to invent a second curve),
  max_interval=90.0, same 600s CHECK_TIMEOUT. Not a lower initial interval: CI checks
  realistically never resolve in under 20-30 seconds (runner startup and checkout alone
  usually exceed that), so starting faster doesn't buy real responsiveness, only spends
  calls on a window where the answer is predictable. This schedule works out to ~10 total
  polls per full-duration wait, versus 20 under the old flat interval — verify this
  arithmetic directly in a test rather than trusting it by inspection.
- Add jitter (±10%) to each computed interval — more important here than in Step 13's
  session polling, since multiple PRs (worker, QA, the Step 6.1 integrator) can be
  check-waiting concurrently against GitHub's single shared rate limit, not independent
  per-session budgets. Synchronized polling against a shared budget is worse than
  synchronized polling against independent ones.
- Optional: cap the final sleep interval to the exact remaining time before CHECK_TIMEOUT is
  reached, so total wait never exceeds 600s even slightly. Not required — the original flat-
  interval code didn't guarantee this either, it only appeared to by coincidence of 600 being
  evenly divisible by 30 — but cheap to add if you'd rather have the tighter guarantee.
- Add the @_retry decorator to merge_pull_request() in clients/github.py, matching the same
  pattern already used on the other methods in that file.
- Tests: mock two consecutive transient 5xx responses on merge_pull_request() and confirm the
  third call succeeds, directly validating @_retry's actual configured behavior. Separately,
  test the backoff schedule itself — assert the actual sequence of intervals (or total call
  count over a simulated full-duration wait) matches the intended curve, not just that the
  function eventually returns. Location: follow whatever test conventions the rest of this
  project already uses — not yet confirmed against this codebase specifically, so check
  first rather than assume a structure.

CONSTRAINTS
- Don't change what counts as a passing/failing check set — only how often status gets
  checked and whether the merge call itself retries on transient failure.
- Don't build webhook infrastructure as part of this step — that's a bigger, separate
  decision if you want it later.

ACCEPTANCE CRITERIA
- A check-wait that resolves quickly (checks complete within the first poll or two) is
  detected with comparable or better latency than the old flat-30s version, not worse.
- A full-duration (timeout-length) check-wait uses ~10 total polls, versus the old flat
  interval's 20 — verify against the actual number, not just "fewer than before."
- A simulated transient 5xx on the merge call itself now retries instead of failing outright.
```

---

## Step 15 — Fix the MCP server's per-call event loop creation

```
Stop creating and tearing down a fresh event loop on every MCP tool call, and stop blocking
the entire server on long-running tool calls.

CONTEXT
Every tool in mcp/server.py follows the same pattern: a synchronous function (not async def)
defines an inner async _run() coroutine and executes it via asyncio.run(_run()). asyncio.run()
creates a new event loop, runs the coroutine to completion, and tears the loop down — for
every single tool invocation. For most tools (jat_list_sources, jat_get_session, etc.) this is
wasteful but not damaging, since they return quickly. jat_run_session is a different story: it
calls session_runner.run_session(), which polls for up to SESSION_TIMEOUT (30 minutes). Since
that entire call happens inside one asyncio.run(), whatever process is hosting this MCP server
is occupied by that single call for up to 30 minutes. Depending on how the underlying
transport dispatches concurrent requests, that plausibly means no other tool call — including
jat_get_session or jat_list_sessions, the tools you'd actually want to use to check on
progress while a long session runs — can be served until it returns. That defeats the purpose
of having separate observability tools available during a long-running session.

REQUIREMENTS
- Convert each @mcp.tool()-decorated function to an actual async def, and await client calls
  directly instead of wrapping them in an inner _run() coroutine passed to asyncio.run().
  Confirm the installed version of FastMCP (mcp.server.fastmcp) supports async tool functions
  directly before assuming — it very likely does, but verify against what's actually pinned
  in this project rather than against general knowledge of the library.
- For jat_run_session specifically: once this is async, confirm whether long-running tool
  calls actually block other concurrent tool calls under this server's transport (stdio vs.
  SSE — inspect how mcp.run() is invoked and what transport this deploys with). If they do,
  consider whether jat_run_session should return immediately with a session id and let the
  caller poll jat_get_session separately, rather than blocking the tool call for the session's
  entire duration. That's a bigger behavioral change than the async conversion alone — treat
  it as a separate decision if the async fix by itself doesn't resolve the blocking.
- _get_jules(), _get_github(), and _get_db() currently construct a fresh client on every tool
  call and close it at the end of that single call — unlike account_pool.py's per-account
  client caching, there's no reuse across calls here. Given MCP tool calls are likely
  interactive rather than high-frequency, this is lower priority than the event-loop fix
  above — worth a look while you're in this file, not worth its own step.

CONSTRAINTS
- Don't change what each tool returns or its parameters — this is about how the call executes
  internally, not its external contract, except for the jat_run_session blocking-call decision
  above if it turns out to be necessary.

ACCEPTANCE CRITERIA
- No tool function calls asyncio.run() internally.
- With jat_run_session in flight against a long-running session, jat_get_session and
  jat_list_sessions both still return promptly when called — verify this directly with a
  concurrent test, not just code review, since it's the actual behavior in question.
```

---

## Step 16 — Cache prompt_builder.py's template reads

```
Stop re-reading rules.xml, gates.xml, anti_patterns.xml, environment.xml, and context.xml
from disk on every single session dispatch, and stop doing blocking file I/O inside an async
codebase.

CONTEXT
build_session_prompt() calls _load_template() once per enabled injection flag (inject_rules,
inject_gates, inject_anti_patterns, inject_environment, inject_context — all default to true
in config.example.json), and _load_template() does a synchronous path.read_text() every time,
with zero caching. That's up to five blocking disk reads on every worker, orchestrator, and
QA session dispatch — and build_session_prompt() is itself a plain synchronous function,
called from async code (session_runner.py), so those reads block the event loop for their
duration rather than yielding to other concurrent work. rules.xml, gates.xml, and
anti_patterns.xml have no per-call substitution at all — they're loaded and used completely
unchanged every time. environment.xml and context.xml do have per-call substitution (plan
tier, daily/concurrent usage, dependency context), but only the substitution needs to happen
per call — the template text underneath doesn't.

Also confirmed, not just suspected: build_session_prompt() imports
core.config_loader.load_config() on every call, and load_config() re-reads and re-parses
config.json from disk on every single call — no caching at all. That's a sixth redundant disk
read per dispatch in the same function, worth fixing alongside this rather than separately.

Separately: prompt_builder.py also defines build_agent_xml_prompt(), a second prompt-building
function using xml_agent_template.xml, with parameters (agent_index, total_agents,
files_scope, acceptance_criteria) that look like per-agent prompt construction for a
decomposed plan. Confirmed: plan_executor.py does not call it — create_jules_session() there
takes a raw prompt string directly, with no reference to prompt_builder.py at all, which is
itself part of why System B's sessions skip rules/gates/context injection (see Step 4). If
nothing else calls build_agent_xml_prompt() either, it may simply be dead code — worth a
quick grep before deciding whether it needs the same caching fix.

REQUIREMENTS
- Cache the raw template text for all five files in memory the first time each is loaded (a
  module-level dict keyed by filename is enough), and read from the cache on subsequent calls
  instead of hitting disk again. Substitution (for environment.xml and context.xml) still
  happens per call, against the cached template text.
- Add a way to invalidate the cache — at minimum on process restart is fine given how rarely
  these files change; a file-watch or explicit reload trigger is a nice-to-have, not required.
- Cache config_loader.load_config()'s result the same way — confirmed re-reading from disk on
  every call, not just suspected.
- Grep for build_agent_xml_prompt() across the codebase. If something calls it, apply the
  same template-caching fix there too. If nothing does, leave it alone — fixing dead code
  isn't worth the scope here.

CONSTRAINTS
- The output of build_session_prompt() for a given set of inputs must be byte-for-byte
  identical before and after this change — this is a caching fix, not a behavior change.

ACCEPTANCE CRITERIA
- Calling build_session_prompt() twice in a row with the same flags results in only one disk
  read per template file, not two.
- Output is identical to the pre-change version for the same inputs — add a test that pins
  this down explicitly.
```

---

## Step 17 — File header comments + docs/map.json

```
Give every file a descriptive header explaining its purpose, and generate a machine-readable
map of the whole repository so future agents can navigate it without guessing at paths.

CONTEXT
This entire document is proof of the problem it's solving: every file referenced in it was
found by guessing plausible names, fetching, and correcting course when the guess was wrong.
A description per file plus one map of the repo would have turned most of that into a single
lookup. Run this step last, after everything else in this document — it writes headers into
files that steps 2 through 14, 6.1, and 16 all still modify; running it earlier just means
redoing that work as those steps land.

SCOPE
Applies to every file the project actually authors and maintains: .py, .json (config/data
files, not lock files), .sql, .sh/.bat, .yaml/.yml, and frontend source files. Excludes
dependency directories (node_modules, .venv, site-packages), lock files (uv.lock,
package-lock.json, pnpm-lock.yaml), build output, .git, and anything tool-generated rather
than hand-authored. Use this same exclusion list for both parts below — don't let them drift
apart from each other.

PART A — File header comments

REQUIREMENTS
- Every applicable file gets a header, in whatever comment syntax that file type supports,
  with three consistent parts every time:
    1. One-line summary of what this file is.
    2. What it actually does — main responsibilities, 2-4 sentences or a short list.
    3. How it fits in — what it depends on, what depends on it, or which files it's tightly
       coupled to, when that's not obvious from its location alone.
  Keep this structure identical across every file so the results are scannable and
  comparable, not freeform per file.
- .py files: a module-level docstring at the top, before imports.
- .sql, .sh: native comment syntax for that file type.
- Already-prose .md files (README.md, this document, etc.): skip — they already describe
  themselves. This is for source and config files, not documentation.
- .json files have no native comment syntax — add a reserved top-level key, "_description",
  whose value is the same three-part summary as one string. Use this exact key name
  everywhere. Confirm nothing in the codebase does strict schema validation on these files
  that would reject an unrecognized key; if something does, either allowlist the key there
  or use whatever metadata mechanism that validator already supports instead.

CONSTRAINTS
- Comments and one metadata key only — no behavior changes to any file.
- Don't touch excluded paths.

ACCEPTANCE CRITERIA
- Every non-excluded file has a header matching the three-part structure.
- Every non-excluded .json file has a "_description" key, and the application still starts
  and runs correctly with it present.

PART B — docs/map.json

REQUIREMENTS
- Add a script (wherever this project's existing scripts/ directory convention points) that
  walks the repo, applying Part A's exclusion list, and generates docs/map.json as a flat
  list, one entry per file:
    {
      "path": "src/core/account_pool.py",
      "raw_url": "https://raw.githubusercontent.com/ProjectCoCom/Hirschberg/refs/heads/development/src/core/account_pool.py",
      "description": "<the one-line summary from that file's Part A header>"
    }
  Part A must run first, since "description" is pulled directly from what it wrote — sequence
  these two parts in that order within this step, not in parallel.
- Derive owner/repo/branch for raw_url from git itself (remote origin, current branch) rather
  than hardcoding this repo's current name — the map should regenerate correctly if this
  moves or gets forked again, not need hand-editing every time.
- Make the script safely re-runnable: running it again after files are added, removed, or
  renamed produces a correct, complete map, not a stale or duplicated one.
- Add a check to whichever CI workflow already exists in .github/workflows/ (inspect first)
  that fails if docs/map.json is out of date relative to the actual file tree — otherwise
  this goes stale the first time someone adds a file and forgets to regenerate it, and stops
  being trustworthy for exactly the use case it exists for.

CONSTRAINTS
- docs/map.json is itself generated output — exclude it from Part A's header requirement and
  from its own listing.

ACCEPTANCE CRITERIA
- docs/map.json exists, includes every non-excluded file with a correct path, working raw
  URL, and non-empty description.
- Deleting a file and re-running the script removes it from the map; adding a file and
  re-running adds it, description pulled from that file's own Part A header.
- The CI check fails on a deliberately stale map and passes once regenerated.
```
